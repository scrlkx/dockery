# dockery

A description of this project.

## Commit messages

Commit messages are validated with `gitlint` through `lefthook`.

```bash
pip install -r requirements-dev.txt
lefthook install
```

Format: `type(scope): summary`

Scopes: `connection`, `flatpak`, `ui`, `containers`, `images`, `volumes`, `networks`, `system`
